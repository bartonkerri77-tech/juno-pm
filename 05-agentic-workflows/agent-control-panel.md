# Agent Control Panel · Juno

_Module 5. Generated by `M5 - Agent Control Panel.html`. Paste over `05-agentic-workflows/agent-control-panel.md`._

## Autonomy level

Agent can create a new design project exactly following the default design system but cannot override an existing one
Agent can create slides in My Drive but cannot create in a Shared folder

## Controls

- **Kill switch:** max_steps 10. Abort if tool fails 2x in a row. Abort if time exceeds 15 mins.
- **Rate / cost caps:** _(missing)_
- **Escalate-on-stuck:** After 3 failed attempts in Claude to create a new project, abandon the attempts 
After 3 failed attempts to create a slide deck, abandon the attempts

## Monitoring

**Confidence thresholds (map to actions):**

>80% on working prototypes for all three features requiring prototypes  
70-79% on working prototypes for all three features requiring prototypes
<70 on working prototypes for all three features requiring prototypes

**Checkpoints:**

Any PII on prototypes or slides must be anonymized. Any questions on PII - send to PM to review. Any questions on whether something should be prototyped - send to PM to review

**North Star (re-read every loop):**

You are Juno. You are responsible for getting the executives ready for what will be in scope for the next sprint. You are also ensuring that submitted features are reflected accurately on slides. You are also getting working prototypes started so that team members can visualize the front end changes.

## Permissions

READ: Submitted logic from the backlog prioritization list. 
Write: Claude Design Projects 
Write: Google Slides

## Self-review

- [ ] Stop conditions include max_steps + wall-clock timeout.
- [ ] Tool outputs include a confidence/score field per retrieval tool.
- [ ] Confidence thresholds map to actions, not just labels.
- [ ] North Star is one sentence, re-read every loop.
- [ ] Each rule of engagement names something the agent CANNOT do.
